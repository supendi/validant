{
  "name": "tsjson-validation",
  "version": "0.0.20",
  "description": "Simple json validator by using user-defined validation rules",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "/dist"
  ],
  "scripts": {
    "test": "jest",
    "clean-dist": "rm -rf ./dist",
    "clean-test": "yarn clean-dist && yarn test",
    "deep-clean": "yarn clean-dist && rm -rf node_modules",
    "deep-clean-test": "yarn deep-clean && yarn install && yarn test",
    "build": "yarn clean-dist && yarn install && yarn test && tsc",
    "release": "yarn deep-clean && yarn install && yarn test && tsc",
    "version-patch": "yarn build && npm version patch && git push"
  },
  "devDependencies": {
    "@babel/core": "^7.13.16",
    "@babel/preset-env": "^7.13.15",
    "@babel/preset-typescript": "^7.13.0",
    "@types/jest": "^29.5.12",
    "babel-jest": "^26.6.3",
    "jest": "^26.6.3"
  },
  "keywords": [
    "validator",
    "validation",
    "json validator",
    "json validation",
    "js validator",
    "js validation",
    "ts validator",
    "typescript validator",
    "ts validation",
    "javascript validator"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/supendi/tsjson-validation.git"
  },
  "author": "github.com/supendi",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/supendi/tsjson-validation/issues"
  },
  "homepage": "https://github.com/supendi/tsjson-validation#readme"
}